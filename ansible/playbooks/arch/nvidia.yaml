---
- name: Install NVIDIA Drivers for RTX 3080 (Wayland/Hyprland)
  hosts: localhost
  become: true

  vars:
    nvidia_packages:
      - nvidia-open
      - nvidia-utils
      - lib32-nvidia-utils

  tasks:
    - name: Configure mkinitcpio with kms hook (no NVIDIA modules)
      ansible.builtin.lineinfile:
        path: /etc/mkinitcpio.conf
        regexp: '^HOOKS='
        line: 'HOOKS=(base udev autodetect microcode modconf kms block filesystems keyboard fsck)'
      notify: Regenerate initramfs

    - name: Set empty MODULES in mkinitcpio
      ansible.builtin.lineinfile:
        path: /etc/mkinitcpio.conf
        regexp: '^MODULES='
        line: 'MODULES=()'
      notify: Regenerate initramfs

    - name: Disable GSP firmware to prevent Ampere GPU hangs
      ansible.builtin.copy:
        dest: /etc/modprobe.d/nvidia.conf
        content: |
          # Disable GSP firmware - required for Ampere (RTX 3080) stability
          options nvidia NVreg_EnableGpuFirmware=0
        mode: '0644'

    - name: Install NVIDIA driver packages with 32-bit support
      community.general.pacman:
        name: "{{ nvidia_packages }}"
        state: present
        update_cache: true
        extra_args: --noconfirm

    - name: Enable DRM kernel mode setting for Wayland
      ansible.builtin.lineinfile:
        path: /etc/default/grub
        regexp: '^GRUB_CMDLINE_LINUX_DEFAULT='
        line: 'GRUB_CMDLINE_LINUX_DEFAULT="quiet nvidia-drm.modeset=1 nvidia_drm.fbdev=0"'
      notify: Update GRUB

    - name: Create systemd DRI device dependency for display manager
      ansible.builtin.copy:
        dest: /etc/udev/rules.d/99-systemd-dri-devices.rules
        content: |
          ACTION=="add", KERNEL=="card*", SUBSYSTEM=="drm", TAG+="systemd"
        mode: '0644'

    - name: Create NVIDIA application profiles directory
      ansible.builtin.file:
        path: /etc/nvidia/nvidia-application-profiles-rc.d
        state: directory
        mode: '0755'

    - name: Configure VRAM optimization for Wayland compositors
      ansible.builtin.copy:
        dest: /etc/nvidia/nvidia-application-profiles-rc.d/50-limit-free-buffer-pool-in-wayland-compositors.json
        content: |
          {
              "rules": [
                  {
                      "pattern": {
                          "feature": "procname",
                          "matches": "Hyprland"
                      },
                      "profile": "Limit free buffer pool on Wayland compositors"
                  }
              ],
              "profiles": [
                  {
                      "name": "Limit free buffer pool on Wayland compositors",
                      "settings": [
                          {
                              "key": "GLVidHeapReuseRatio",
                              "value": 0
                          }
                      ]
                  }
              ]
          }
        mode: '0644'

  handlers:
    - name: Regenerate initramfs
      ansible.builtin.command:
        cmd: mkinitcpio -P
      changed_when: true

    - name: Update GRUB
      ansible.builtin.command:
        cmd: grub-mkconfig -o /boot/grub/grub.cfg
      changed_when: true
