# Background

- This setup is new arch install using hyprland

# Requirements

- The user wants the Adwaita cursor
- The user wants apps like zen browser and nemo to understand that the OS is configured for dark mode (right now they are showing up as light, which is not desired)
- Keep configs simple and avoid bloat

# Non Functional Requirements

- Keep the human in the loop
- No commits to git
