---
- name: Install Discord
  hosts: localhost
  become: true # Necessary to run with root privileges
  tasks:
    - name: Install Discord
      community.general.pacman:
        name: discord
        state: present
        update_cache: true
